<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->


    <CheckBoxPreference
        android:key="@string/background_sync"
        android:title="@string/pref_sync_in_background"
        android:summary="@string/pref_sync_in_background_summary"
        android:defaultValue="true"
        android:enabled="false"/>

    <CheckBoxPreference
        android:key="@string/pref_key_auto_upload_screenshots"
        android:title="@string/pref_auto_upload_screenshots"
        android:summary="@string/pref_auto_upload_screenshots_summary"
        android:defaultValue="true"
        android:enabled="false"/>

    <ListPreference
        android:key="@string/pref_key_sharable_url"
        android:title="@string/pref_shareable_link_type"
        android:defaultValue="0"
        android:entries="@array/pref_shareable_link_type_titles"
        android:entryValues="@array/pref_shareable_link_type_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"/>

</PreferenceScreen>
